org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Added by the Flutter template; both keep the current AGP on the DSL and Kotlin
# integration this project's build files are written against.
android.newDsl=false
android.builtInKotlin=false
# Kotlin incremental compilation writes caches it then fails to close on this
# toolchain; the builds it saves are not worth a build that does not complete.
kotlin.incremental=false
