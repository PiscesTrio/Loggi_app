<manifest xmlns:android="http://schemas.android.com/apk/res/android">
   <application

        android:label="Loggi"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>


    <!-- Required to fetch map tiles and reach the API. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Kept deliberately: flutter_nb_net checks reachability through
         connectivity_plus before every request. The plugin declares this itself,
         so the entry here is redundant but documents why it is needed. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- The other seven permissions that used to live here came verbatim from the
         AMap SDK's setup docs (their comments described AMap's "blue dot"
         positioning and its crash-report cache): COARSE_LOCATION, READ_PHONE_STATE,
         ACCESS/CHANGE_WIFI_STATE, READ/WRITE_EXTERNAL_STORAGE and WRITE_SETTINGS.
         AMap is gone and nothing in the app requests a location or touches external
         storage, so they were only widening the permission surface — WRITE_SETTINGS
         in particular is a special-access permission this app never had a use for. -->


    <!-- Provide required visibility configuration for API level 30 and above -->
<queries>
  <!-- If your app checks for SMS support -->
  <intent>
    <action android:name="android.intent.action.VIEW" />
    <data android:scheme="sms" />
  </intent>
  <!-- If your app checks for call support -->
  <intent>
    <action android:name="android.intent.action.VIEW" />
    <data android:scheme="tel" />
  </intent>
</queries>
</manifest>
